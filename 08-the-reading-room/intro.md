# 🔑 The Reading Room

A new reporting workload, `report-api`, has just been deployed to the
`rbac-lab` namespace. It runs as its own ServiceAccount,
`report-reader` — but nobody has granted that identity any
permissions yet.

You have administrative access to the cluster. Your job is to design
and implement the authorization model it needs — no more, and no
less.

Click **Start** when you're ready to begin.
